displayName: "Firebase JWT token validation"
import: github.com/s00rk/firebase-verify-token
type: middleware
summary: "Validate JWT token generated by Firebase and add claims and user id to the header like fb-userid and fbclaim-<key>"
testData:
  projectid: projectid_firebase_test